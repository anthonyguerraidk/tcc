MADE FOR LINUX

these two scripts start (startSpot.sh) and stop (stopSpot.sh) a local network (hotspot) using a wifi dongle as the acess point and the ethernet interface as the source gateway
the gateway is set as 192.168.50.1
